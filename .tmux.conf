# ─────────────────────────────
# 1. Core behavior / defaults
# ─────────────────────────────
# unbind C-b
# set -g prefix C-a
# bind C-a send-prefix
set -g mouse on
set -g mode-keys vi
set -g history-limit 10000
set -g default-terminal "tmux-256color"
set-option -sa terminal-features ',xterm-256color:RGB'

# ─────────────────────────────
# 2. Plugins (TPM, etc.)
# ─────────────────────────────
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-resurrect'
#
# set -g @plugin "tmux-plugins/tmux-online-status"
# set -g @plugin "MunifTanjim/tmux-mode-indicator"

# set -g @plugin "nordtheme/tmux"
set -g @plugin 'rose-pine/tmux'
set -g @rose_pine_variant 'main' # Options are 'main', 'moon' or 'dawn'

set -g @plugin 'tmux-plugins/tmux-continuum'

# set -g @plugin 'jimeh/tmux-themepack'
# set -g @themepack 'powerline/default/green'
# set -g @themepack 'basic'
# set -g @tokyo-night-tmux_theme storm    # storm | day | default to 'night'
# set -g @tokyo-night-tmux_transparent 1  # 1 or 1


run '~/.tmux/plugins/tpm/tpm'

# ─────────────────────────────
# 3. Appearance (overrides plugins)
# ─────────────────────────────
# set -g status on
# set -g status-style bg=black,fg=red
# set -g status-left "#[fg=green] #S #[fg=white]|"
# set -g status-right "#[fg=white]%Y-%m-%d #[fg=red]%H:%M "
# set -g pane-border-style fg=red
# set -g pane-active-border-style fg=brightred
# set -g message-style bg=black,fg=red

# ─────────────────────────────
# 4. Keybindings
# ─────────────────────────────
# bind r source-file ~/.tmux.conf \; display-message "Config reloaded!"
